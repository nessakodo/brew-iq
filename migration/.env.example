# BrewIQ Environment Variables

# ============================================
# SUPABASE CONFIGURATION
# ============================================
# Get these from: Supabase Dashboard → Settings → API

# Your new Supabase project URL
VITE_SUPABASE_URL=https://YOUR-PROJECT-ID.supabase.co

# Your new Supabase anon/public key
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================
# EDGE FUNCTION SECRETS (Set via Supabase CLI)
# ============================================
# These are NOT set in .env files - they're set using:
# supabase secrets set OPENAI_API_KEY=your-key-here

# OpenAI API Key for AI trivia generation and marketing images
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# ============================================
# DEVELOPMENT ONLY
# ============================================
# These values are only used for local development
# For production, set environment variables in Vercel/Netlify

# Optional: Local Supabase instance
# VITE_SUPABASE_URL=http://localhost:54321
# VITE_SUPABASE_ANON_KEY=your-local-anon-key

# ============================================
# INSTRUCTIONS
# ============================================

# 1. Copy this file to .env.local:
#    cp migration/.env.example .env.local

# 2. Fill in your Supabase URL and anon key from:
#    Supabase Dashboard → Settings → API

# 3. For edge functions, set secrets using Supabase CLI:
#    supabase secrets set OPENAI_API_KEY=your-openai-key

# 4. For production deployment:
#    - Vercel: Add env vars in project settings
#    - Netlify: Use netlify env:set command

# 5. NEVER commit .env.local to git!
#    (It's already in .gitignore)

# ============================================
# COST NOTES
# ============================================

# Supabase: Free tier includes 500MB database, 5GB bandwidth
# OpenAI API Costs:
#   - GPT-4o-mini: ~$0.15 per 1M tokens (~$0.002 per trivia generation)
#   - DALL-E 3: ~$0.04 per 1024x1024 image
# Estimated monthly cost: $0-5 for typical usage
